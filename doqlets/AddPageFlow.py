# Add page flow

#page title, page description, section requests

#create a page plan, create a basic section list
#section - section name, what the section contains, any formatting instructions

def add_page(page_title, page_description, section_requests):
   
   #check for existing duplicate page

   #create page rules

   #create page sections - each section has a title, rules

   #return the page id

   return page_id